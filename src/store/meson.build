# store adapter build

# Phase 0: standalone spike binary — proves the subprocess pattern works
spike_sources = files('spike-nix-add.c')

executable(
  'spike-nix-add',
  spike_sources,
  install : false,
)
